# =============================================================
# Progressive Validation Configuration for 3D-Ising-CFT-Bootstrap
# =============================================================
#
# Copy this file to the project root as .env.validation
# and customize the settings below.
#
# =============================================================

# Enable/Disable Progressive Validation
ENABLE_PROGRESSIVE_VALIDATION=1

# Polling Configuration
POLL_INTERVAL=60  # seconds between directory polls

# Anomaly Detection Thresholds
ANOMALY_THRESHOLD_WARNING=10   # tasks before warning alert
ANOMALY_THRESHOLD_CRITICAL=20  # tasks before critical alert

# Bound Tolerances (for detecting "stuck" values)
LOWER_BOUND_TOLERANCE=0.01  # detect values within 0.5 ± 0.01 (unitarity floor)
UPPER_BOUND_TOLERANCE=0.01  # detect values within 2.5 ± 0.01 (upper bound)

# Auto-Cancellation (DANGEROUS - use with caution)
# Set to 1 to automatically cancel jobs when critical anomaly detected
# Default: 0 (conservative - alerts only, no cancellation)
CANCEL_ON_CRITICAL=0
